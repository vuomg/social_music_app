rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Chỉ cần đăng nhập là có thể đọc và ghi mọi file
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
